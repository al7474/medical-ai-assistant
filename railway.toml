# Railway Configuration for Medical AI Assistant
# This tells Railway how to deploy each service

[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "echo 'Use individual service configs'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
